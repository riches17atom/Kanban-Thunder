# Django Security & Debug
DEBUG=True
SECRET_KEY=django-insecure-k@nb@n-thund3r-s3cr3t-k3y-change-this-in-production!
ALLOWED_HOSTS=127.0.0.1,localhost

# Local PostgreSQL Database Configuration
DB_NAME=thunder_db
DB_USER=postgres
DB_PASSWORD=postgres
DB_HOST=localhost
DB_PORT=5432

# Or specify a direct database URL (takes precedence if provided, e.g. for Render or Neon)
# DATABASE_URL=postgresql://postgres:postgres@localhost:5432/thunder_db

# Google OAuth2 Authentication (Optional - for Google Sign-In)
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# CORS Configuration (Frontend URLs)
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://localhost:5174,http://localhost:3000,http://127.0.0.1:5173
