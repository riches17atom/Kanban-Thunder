# Ansible Inventory File
# Replace <EC2_PUBLIC_IP> with the IP output by Terraform

[webservers]
kanban_server ansible_host=<EC2_PUBLIC_IP> ansible_user=ubuntu ansible_ssh_private_key_file=~/.ssh/kanban_aws_key

[all:vars]
ansible_python_interpreter=/usr/bin/python3
