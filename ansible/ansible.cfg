[defaults]
inventory = inventory.ini
remote_user = ubuntu
private_key_file = ~/.ssh/kanban_aws_key
host_key_checking = False
retry_files_enabled = False
timeout = 30
stdout_callback = yaml

[privilege_escalation]
become = True
become_method = sudo
become_user = root
become_ask_pass = False
